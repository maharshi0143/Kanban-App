# Docker Notes

This directory is reserved for optional Docker-related helpers (override compose files, scripts, and deployment manifests).

The active runtime configuration is currently defined at the project root:

- `Dockerfile`
- `docker-compose.yml`
